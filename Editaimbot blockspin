if not game:IsLoaded() then game.Loaded:Wait() end

local Players = game:GetService("Players")
local LocalPlayer = Players.LocalPlayer
local Camera = workspace.CurrentCamera
local RunService = game:GetService("RunService")
local UserInputService = game:GetService("UserInputService")
local CoreGui = game:GetService("CoreGui")
local ReplicatedStorage = game:GetService("ReplicatedStorage")

-- [[ ระบบป้องกัน: รายชื่อคนที่จะถูกคุ้มครอง ]]
local protectedNames = {
    "jvar63icxg19",
}

-- [[ 1. ระบบแก้กล้องบนรถ และ มุมกล้องบนพื้น ]]
local fixVehicleCamEnabled = true

RunService.RenderStepped:Connect(function()
    if not fixVehicleCamEnabled then return end
    local character = LocalPlayer.Character
    if not character then return end
    local humanoid = character:FindFirstChildOfClass("Humanoid")
    
    if humanoid and humanoid.SeatPart then
        pcall(function()
            Camera.CameraType = Enum.CameraType.Custom
            humanoid.CameraOffset = Vector3.new(0, 0.8, 0)
            LocalPlayer.CameraMinZoomDistance = 8
        end)
    else
        pcall(function()
            humanoid.CameraOffset = Vector3.new(1.2, 0.5, 0)
            LocalPlayer.CameraMinZoomDistance = 0.5
        end)
    end
end)

-- [[ 2. ระบบ Item ESP & 3D Highlight ESP (สีตามเลือด) ]]
local ItemESP_Enabled = true
local HighlightESP_Enabled = true 
local HideCommonItems = true 
local WeaponDB = {}

local RARITY_COLORS = {
    ["Common"] = Color3.fromRGB(255, 255, 255),
    ["Uncommon"] = Color3.fromRGB(99, 255, 52),
    ["Rare"] = Color3.fromRGB(51, 170, 255),
    ["Epic"] = Color3.fromRGB(237, 44, 255),
    ["Legendary"] = Color3.fromRGB(255, 150, 0),
    ["Omega"] = Color3.fromRGB(255, 20, 51),
}

local function generateUniqueKey(tool)
    if not tool or not tool:IsA("Tool") then return nil end
    local itemId = tool:GetAttribute("ItemId") or tool:GetAttribute("Id")
    if itemId and itemId ~= "" and (typeof(itemId) == "string" or typeof(itemId) == "number") then
        return "ITEMID_" .. tostring(itemId)
    end
    local partsData = {}
    for _, part in ipairs(tool:GetDescendants()) do
        if part:IsA("SpecialMesh") and part.MeshId and part.MeshId ~= "" and part.MeshId ~= "rbxassetid://" then
            table.insert(partsData, "MESH_"..part.MeshId.."|TEX_"..(part.TextureId or "NOTEX"))
        elseif part:IsA("MeshPart") and part.MeshId and part.MeshId ~= "" and part.MeshId ~= "rbxassetid://" then
            table.insert(partsData, "MESH_"..part.MeshId.."|TEX_"..(part.TextureID or "NOTEX"))
        elseif part:IsA("Decal") and part.Texture and part.Texture ~= "" and part.Texture ~= "rbxassetid://" then
            table.insert(partsData, "DECAL_"..part.Texture)
        elseif part:IsA("Part") then
            table.insert(partsData, "PART_"..part.Name.."_"..part.Size.X.."x"..part.Size.Y.."x"..part.Size.Z)
        end
    end
    if #partsData > 0 then
        table.sort(partsData)
        return "MESHKEY_" .. table.concat(partsData, ";")
    end
    local displayName = tool:GetAttribute("DisplayName") or tool.Name
    local toolName = tool.Name
    local rarity = tool:GetAttribute("RarityName") or tool:GetAttribute("Rarity") or "Unknown"
    return "NAME_" .. displayName .. "_" .. toolName .. "_" .. rarity
end

local function registerItems(folder)
    for _, tool in ipairs(folder:GetDescendants()) do
        if not tool:IsA("Tool") then continue end
        local key = generateUniqueKey(tool)
        if not key then continue end
        local displayName = tool:GetAttribute("DisplayName") or tool.Name
        local rarity = tool:GetAttribute("RarityName") or tool:GetAttribute("Rarity") or "Common"
        WeaponDB[key] = {
            Name = displayName,
            Rarity = rarity,
            ToolName = tool.Name,
            Key = key
        }
    end
end

pcall(function()
    local itemsFolder = ReplicatedStorage:WaitForChild("Items", 5)
    if itemsFolder then registerItems(itemsFolder) end
    for _, obj in ipairs(ReplicatedStorage:GetChildren()) do
        if obj:IsA("Folder") and (obj.Name:find("Weapon") or obj.Name:find("Item") or obj.Name:find("Tool")) then
            registerItems(obj)
        end
    end
    registerItems(game:GetService("StarterPack"))
end)

local function getWeaponInfo(tool)
    if not tool or not tool:IsA("Tool") then return nil end
    local key = generateUniqueKey(tool)
    return WeaponDB[key] or {
        Name = tool.Name,
        Rarity = tool:GetAttribute("RarityName") or tool:GetAttribute("Rarity") or "Common",
        ToolName = tool.Name,
        Key = key
    }
end

if CoreGui:FindFirstChild("ItemESP_ScreenGui") then
    CoreGui.ItemESP_ScreenGui:Destroy()
end
for _, v in ipairs(CoreGui:GetChildren()) do
    if v.Name == "PlayerHighlightESP" then v:Destroy() end
end

local ItemGui = Instance.new("ScreenGui", CoreGui)
ItemGui.Name = "ItemESP_ScreenGui"
ItemGui.IgnoreGuiInset = true

local ItemFrames = {}
local Highlights = {}

local function getHealthColor(health, maxHealth)
    local pct = math.clamp(health / maxHealth, 0, 1)
    if pct > 0.6 then
        return Color3.fromRGB(0, 255, 0) -- เขียว
    elseif pct > 0.3 then
        return Color3.fromRGB(255, 255, 0) -- เหลือง
    else
        return Color3.fromRGB(255, 0, 0) -- แดง
    end
end

local function createPlayerESP(player)
    if player == LocalPlayer or ItemFrames[player] then return end

    local containerFrame = Instance.new("Frame", ItemGui)
    containerFrame.Name = "ItemContainer_" .. player.Name
    containerFrame.Size = UDim2.new(0, 200, 0, 150)
    containerFrame.AnchorPoint = Vector2.new(0.5, 0)
    containerFrame.BackgroundTransparency = 1
    containerFrame.Visible = false

    local layout = Instance.new("UIListLayout", containerFrame)
    layout.Padding = UDim.new(0, 1)
    layout.HorizontalAlignment = Enum.HorizontalAlignment.Center
    layout.VerticalAlignment = Enum.VerticalAlignment.Top
    layout.SortOrder = Enum.SortOrder.LayoutOrder

    ItemFrames[player] = {
        Frame = containerFrame,
        Tools = {}
    }

    local highlight = Instance.new("Highlight")
    highlight.Name = "PlayerHighlightESP"
    highlight.Adornee = nil
    highlight.FillTransparency = 0.5
    highlight.OutlineTransparency = 0
    highlight.FillColor = Color3.fromRGB(0, 255, 0)
    highlight.OutlineColor = Color3.fromRGB(255, 255, 255)
    highlight.Parent = CoreGui
    Highlights[player] = highlight

    local function updateItems()
        if not ItemESP_Enabled or not ItemFrames[player] then return end
        local frame = ItemFrames[player].Frame
        for _, child in ipairs(frame:GetChildren()) do
            if child:IsA("TextLabel") then child:Destroy() end
        end

        local currentTools = {}
        local function scan(folder)
            if not folder then return end
            for _, tool in ipairs(folder:GetChildren()) do
                if tool:IsA("Tool") and tool.Name ~= "Fists" then
                    local info = getWeaponInfo(tool)
                    if info then
                        if not (HideCommonItems and info.Rarity == "Common") then
                            table.insert(currentTools, info)
                        end
                    end
                end
            end
        end

        local char = player.Character
        if char then
            scan(char)
            local backpack = player:FindFirstChild("Backpack")
            if backpack then scan(backpack) end
        end

        for i, info in ipairs(currentTools) do
            local txt = Instance.new("TextLabel")
            txt.Parent = frame
            txt.Size = UDim2.new(1, 0, 0, 11)
            txt.BackgroundTransparency = 1
            txt.Font = Enum.Font.SourceSansBold
            txt.TextSize = 9
            txt.Text = "[" .. tostring(info.Name) .. "]"
            txt.LayoutOrder = i

            local color = RARITY_COLORS[info.Rarity] or Color3.fromRGB(255, 255, 255)
            txt.TextColor3 = color
            
            local stroke = Instance.new("UIStroke")
            stroke.Color = Color3.fromRGB(0, 0, 0)
            stroke.Thickness = 1
            stroke.Parent = txt
        end
    end

    local function bindCharacter(char)
        if not char then return end
        updateItems()
        if Highlights[player] then
            Highlights[player].Adornee = char
        end
        local backpack = player:FindFirstChild("Backpack")
        if backpack then
            backpack.ChildAdded:Connect(updateItems)
            backpack.ChildRemoved:Connect(updateItems)
        end
        char.ChildAdded:Connect(function(child)
            if child:IsA("Tool") then task.defer(updateItems) end
        end)
        char.ChildRemoved:Connect(function(child)
            if child:IsA("Tool") then task.defer(updateItems) end
        end)
    end

    if player.Character then bindCharacter(player.Character) end
    player.CharacterAdded:Connect(bindCharacter)
end

for _, p in ipairs(Players:GetPlayers()) do
    if p ~= LocalPlayer then createPlayerESP(p) end
end
Players.PlayerAdded:Connect(function(p)
    if p ~= LocalPlayer then createPlayerESP(p) end
end)
Players.PlayerRemoving:Connect(function(p)
    if ItemFrames[p] then
        ItemFrames[p].Frame:Destroy()
        ItemFrames[p] = nil
    end
    if Highlights[p] then
        Highlights[p]:Destroy()
        Highlights[p] = nil
    end
end)

RunService.RenderStepped:Connect(function()
    -- อัปเดตตำแหน่ง Item ESP
    if ItemESP_Enabled then
        for player, data in pairs(ItemFrames) do
            local char = player.Character
            if char and char:FindFirstChild("HumanoidRootPart") then
                local root = char.HumanoidRootPart
                local hum = char:FindFirstChildOfClass("Humanoid")
                
                if hum and hum.Health > 0 then
                    local footPos3D = root.Position - Vector3.new(0, 3.2, 0)
                    local screenPos, onScreen = Camera:WorldToViewportPoint(footPos3D)

                    if onScreen then
                        data.Frame.Position = UDim2.new(0, screenPos.X, 0, screenPos.Y)
                        data.Frame.Visible = true
                    else
                        data.Frame.Visible = false
                    end
                else
                    data.Frame.Visible = false
                end
            else
                data.Frame.Visible = false
            end
        end
    end

    -- อัปเดตสีและความสว่างของ Highlight ตามเลือด
    for player, hl in pairs(Highlights) do
        local char = player.Character
        if char and char:FindFirstChildOfClass("Humanoid") and HighlightESP_Enabled then
            local hum = char.Humanoid
            if hum.Health > 0 then
                hl.Adornee = char
                local col = getHealthColor(hum.Health, hum.MaxHealth)
                hl.FillColor = col
                hl.OutlineColor = col
                hl.Enabled = true
            else
                hl.Enabled = false
            end
        else
            hl.Enabled = false
        end
    end
end)

local AimbotSettings = {
    Enabled = false,
    StrongLock = false,
    WallCheck = true,
    LockPart = "Head",
    FOV = 160,
    ShowFOV = true,
    HideMyNameOnly = true,
    IsAiming = false,
    ExcludedPlayers = {}
}

if CoreGui:FindFirstChild("SimpleAimbotUI") then CoreGui.SimpleAimbotUI:Destroy() end
if CoreGui:FindFirstChild("MobileFOVGui") then CoreGui.MobileFOVGui:Destroy() end
if CoreGui:FindFirstChild("ExcludeMenuGui") then CoreGui.ExcludeMenuGui:Destroy() end
if CoreGui:FindFirstChild("GunModMenuGui") then CoreGui.GunModMenuGui:Destroy() end
if CoreGui:FindFirstChild("PlayerInspectorGui") then CoreGui.PlayerInspectorGui:Destroy() end

local FOVGui = Instance.new("ScreenGui", CoreGui)
FOVGui.Name = "MobileFOVGui"
FOVGui.IgnoreGuiInset = true

local FOVFrame = Instance.new("Frame", FOVGui)
FOVFrame.AnchorPoint = Vector2.new(0.5, 0.5)
FOVFrame.Position = UDim2.new(0.5, 0, 0.5, 0)
FOVFrame.BackgroundTransparency = 1
Instance.new("UICorner", FOVFrame).CornerRadius = UDim.new(1, 0)

local FOVStroke = Instance.new("UIStroke", FOVFrame)
FOVStroke.Color = Color3.fromRGB(255, 50, 50)
FOVStroke.Thickness = 2
FOVStroke.ApplyStrokeMode = Enum.ApplyStrokeMode.Border

local MainScreenGui = Instance.new("ScreenGui", CoreGui)
MainScreenGui.Name = "SimpleAimbotUI"
MainScreenGui.IgnoreGuiInset = true

local MainFrame = Instance.new("Frame", MainScreenGui)
MainFrame.BackgroundColor3 = Color3.fromRGB(20, 20, 20)
MainFrame.Position = UDim2.new(0.1, 0, 0.1, 0)
MainFrame.Size = UDim2.new(0, 340, 0, 380)
MainFrame.Active = false
Instance.new("UICorner", MainFrame).CornerRadius = UDim.new(0, 12)

local TitleLabel = Instance.new("TextLabel", MainFrame)
TitleLabel.BackgroundTransparency = 1
TitleLabel.Position = UDim2.new(0.05, 0, 0.03, 0)
TitleLabel.Size = UDim2.new(0.9, 0, 0, 30)
TitleLabel.Font = Enum.Font.SourceSansBold
TitleLabel.Text = " Aimbot & ESP by FitHub "
TitleLabel.TextColor3 = Color3.fromRGB(255, 255, 255)
TitleLabel.TextSize = 18
TitleLabel.TextXAlignment = Enum.TextXAlignment.Left

local Dragging, DragStart, StartPos
TitleLabel.InputBegan:Connect(function(input)
    if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
        Dragging = true
        DragStart = input.Position
        StartPos = MainFrame.Position
        input.Changed:Connect(function()
            if input.UserInputState == Enum.UserInputState.End then
                Dragging = false
            end
        end)
    end
end)

UserInputService.InputChanged:Connect(function(input)
    if input.UserInputType == Enum.UserInputType.MouseMovement or input.UserInputType == Enum.UserInputType.Touch then
        if Dragging then
            local delta = input.Position - DragStart
            MainFrame.Position = UDim2.new(StartPos.X.Scale, StartPos.X.Offset + delta.X, StartPos.Y.Scale, StartPos.Y.Offset + delta.Y)
        end
    end
end)

local AimToggleBtn = Instance.new("TextButton", MainFrame)
AimToggleBtn.BackgroundColor3 = Color3.fromRGB(180, 50, 50)
AimToggleBtn.Position = UDim2.new(0.05, 0, 0.13, 0)
AimToggleBtn.Size = UDim2.new(0, 145, 0, 35)
AimToggleBtn.Font = Enum.Font.SourceSansBold
AimToggleBtn.Text = "Aimbot: OFF (Q)"
AimToggleBtn.TextColor3 = Color3.fromRGB(255, 255, 255)
AimToggleBtn.TextSize = 14
Instance.new("UICorner", AimToggleBtn).CornerRadius = UDim.new(0, 8)

AimToggleBtn.MouseButton1Click:Connect(function()
    AimbotSettings.Enabled = not AimbotSettings.Enabled
    if AimbotSettings.Enabled then
        AimToggleBtn.Text = "Aimbot: ON (Q)"
        AimToggleBtn.BackgroundColor3 = Color3.fromRGB(50, 160, 50)
    else
        AimToggleBtn.Text = "Aimbot: OFF (Q)"
        AimToggleBtn.BackgroundColor3 = Color3.fromRGB(180, 50, 50)
    end
end)

local StrongToggleBtn = Instance.new("TextButton", MainFrame)
StrongToggleBtn.BackgroundColor3 = Color3.fromRGB(180, 50, 50)
StrongToggleBtn.Position = UDim2.new(0.53, 0, 0.13, 0)
StrongToggleBtn.Size = UDim2.new(0, 145, 0, 35)
StrongToggleBtn.Font = Enum.Font.SourceSansBold
StrongToggleBtn.Text = "Strong Lock: OFF"
StrongToggleBtn.TextColor3 = Color3.fromRGB(255, 255, 255)
StrongToggleBtn.TextSize = 14
Instance.new("UICorner", StrongToggleBtn).CornerRadius = UDim.new(0, 8)

StrongToggleBtn.MouseButton1Click:Connect(function()
    AimbotSettings.StrongLock = not AimbotSettings.StrongLock
    if AimbotSettings.StrongLock then
        StrongToggleBtn.Text = "Strong Lock: ON"
        StrongToggleBtn.BackgroundColor3 = Color3.fromRGB(50, 160, 50)
    else
        StrongToggleBtn.Text = "Strong Lock: OFF"
        StrongToggleBtn.BackgroundColor3 = Color3.fromRGB(180, 50, 50)
    end
end)

local WallToggleBtn = Instance.new("TextButton", MainFrame)
WallToggleBtn.BackgroundColor3 = Color3.fromRGB(50, 160, 50)
WallToggleBtn.Position = UDim2.new(0.05, 0, 0.25, 0)
WallToggleBtn.Size = UDim2.new(0, 145, 0, 35)
WallToggleBtn.Font = Enum.Font.SourceSansBold
WallToggleBtn.Text = "Wallcheck: ON"
WallToggleBtn.TextColor3 = Color3.fromRGB(255, 255, 255)
WallToggleBtn.TextSize = 14
Instance.new("UICorner", WallToggleBtn).CornerRadius = UDim.new(0, 8)

WallToggleBtn.MouseButton1Click:Connect(function()
    AimbotSettings.WallCheck = not AimbotSettings.WallCheck
    if AimbotSettings.WallCheck then
        WallToggleBtn.Text = "Wallcheck: ON"
        WallToggleBtn.BackgroundColor3 = Color3.fromRGB(50, 160, 50)
    else
        WallToggleBtn.Text = "Wallcheck: OFF"
        WallToggleBtn.BackgroundColor3 = Color3.fromRGB(180, 50, 50)
    end
end)

local HideMyNameBtn = Instance.new("TextButton", MainFrame)
HideMyNameBtn.BackgroundColor3 = Color3.fromRGB(50, 160, 50)
HideMyNameBtn.Position = UDim2.new(0.53, 0, 0.25, 0)
HideMyNameBtn.Size = UDim2.new(0, 145, 0, 35)
HideMyNameBtn.Font = Enum.Font.SourceSansBold
HideMyNameBtn.Text = "Hide My Name: ON"
HideMyNameBtn.TextColor3 = Color3.fromRGB(255, 255, 255)
HideMyNameBtn.TextSize = 14
Instance.new("UICorner", HideMyNameBtn).CornerRadius = UDim.new(0, 8)

HideMyNameBtn.MouseButton1Click:Connect(function()
    AimbotSettings.HideMyNameOnly = not AimbotSettings.HideMyNameOnly
    if AimbotSettings.HideMyNameOnly then
        HideMyNameBtn.Text = "Hide My Name: ON"
        HideMyNameBtn.BackgroundColor3 = Color3.fromRGB(50, 160, 50)
    else
        HideMyNameBtn.Text = "Hide My Name: OFF"
        HideMyNameBtn.BackgroundColor3 = Color3.fromRGB(180, 50, 50)
    end
end)

local ESPToggleBtn = Instance.new("TextButton", MainFrame)
ESPToggleBtn.BackgroundColor3 = Color3.fromRGB(50, 160, 50)
ESPToggleBtn.Position = UDim2.new(0.05, 0, 0.37, 0)
ESPToggleBtn.Size = UDim2.new(0, 145, 0, 35)
ESPToggleBtn.Font = Enum.Font.SourceSansBold
ESPToggleBtn.Text = "Highlight ESP: ON"
ESPToggleBtn.TextColor3 = Color3.fromRGB(255, 255, 255)
ESPToggleBtn.TextSize = 14
Instance.new("UICorner", ESPToggleBtn).CornerRadius = UDim.new(0, 8)

ESPToggleBtn.MouseButton1Click:Connect(function()
    HighlightESP_Enabled = not HighlightESP_Enabled
    if HighlightESP_Enabled then
        ESPToggleBtn.Text = "Highlight ESP: ON"
        ESPToggleBtn.BackgroundColor3 = Color3.fromRGB(50, 160, 50)
    else
        ESPToggleBtn.Text = "Highlight ESP: OFF"
        ESPToggleBtn.BackgroundColor3 = Color3.fromRGB(180, 50, 50)
        for _, hl in pairs(Highlights) do
            if hl then hl.Enabled = false end
        end
    end
end)

local CommonToggleBtn = Instance.new("TextButton", MainFrame)
CommonToggleBtn.BackgroundColor3 = Color3.fromRGB(50, 160, 50)
CommonToggleBtn.Position = UDim2.new(0.53, 0, 0.37, 0)
CommonToggleBtn.Size = UDim2.new(0, 145, 0, 35)
CommonToggleBtn.Font = Enum.Font.SourceSansBold
CommonToggleBtn.Text = "Hide Common: ON"
CommonToggleBtn.TextColor3 = Color3.fromRGB(255, 255, 255)
CommonToggleBtn.TextSize = 14
Instance.new("UICorner", CommonToggleBtn).CornerRadius = UDim.new(0, 8)

CommonToggleBtn.MouseButton1Click:Connect(function()
    HideCommonItems = not HideCommonItems
    if HideCommonItems then
        CommonToggleBtn.Text = "Hide Common: ON"
        CommonToggleBtn.BackgroundColor3 = Color3.fromRGB(50, 160, 50)
    else
        CommonToggleBtn.Text = "Hide Common: OFF"
        CommonToggleBtn.BackgroundColor3 = Color3.fromRGB(180, 50, 50)
    end
    for _, data in pairs(ItemFrames) do
        if data and data.Frame then
            for _, child in ipairs(data.Frame:GetChildren()) do
                if child:IsA("TextLabel") then child:Destroy() end
            end
        end
    end
end)

local FOVBox = Instance.new("TextBox", MainFrame)
FOVBox.BackgroundColor3 = Color3.fromRGB(50, 50, 50)
FOVBox.Position = UDim2.new(0.05, 0, 0.49, 0)
FOVBox.Size = UDim2.new(0, 145, 0, 35)
FOVBox.Font = Enum.Font.SourceSans
FOVBox.Text = tostring(AimbotSettings.FOV)
FOVBox.PlaceholderText = "FOV Size"
FOVBox.TextColor3 = Color3.fromRGB(255, 255, 255)
FOVBox.TextSize = 14
Instance.new("UICorner", FOVBox).CornerRadius = UDim.new(0, 8)

FOVBox.FocusLost:Connect(function()
    local val = tonumber(FOVBox.Text)
    if val then
        AimbotSettings.FOV = val
    else
        FOVBox.Text = tostring(AimbotSettings.FOV)
    end
end)

local ExcludeBtn = Instance.new("TextButton", MainFrame)
ExcludeBtn.BackgroundColor3 = Color3.fromRGB(50, 50, 50)
ExcludeBtn.Position = UDim2.new(0.53, 0, 0.49, 0)
ExcludeBtn.Size = UDim2.new(0, 145, 0, 35)
ExcludeBtn.Font = Enum.Font.SourceSansBold
ExcludeBtn.Text = "ไม่ล็อคเพื่อน"
ExcludeBtn.TextColor3 = Color3.fromRGB(150, 180, 220)
ExcludeBtn.TextSize = 14
Instance.new("UICorner", ExcludeBtn).CornerRadius = UDim.new(0, 8)

local InspectMenuBtn = Instance.new("TextButton", MainFrame)
InspectMenuBtn.BackgroundColor3 = Color3.fromRGB(100, 50, 160)
InspectMenuBtn.Position = UDim2.new(0.05, 0, 0.61, 0)
InspectMenuBtn.Size = UDim2.new(0.9, 0, 0, 40)
InspectMenuBtn.Font = Enum.Font.SourceSansBold
InspectMenuBtn.Text = "👥 เมนูลอบดูของคนในเซิร์ฟเวอร์"
InspectMenuBtn.TextColor3 = Color3.fromRGB(255, 255, 255)
InspectMenuBtn.TextSize = 15
Instance.new("UICorner", InspectMenuBtn).CornerRadius = UDim.new(0, 8)

local GunModBtn = Instance.new("TextButton", MainFrame)
GunModBtn.BackgroundColor3 = Color3.fromRGB(40, 80, 160)
GunModBtn.Position = UDim2.new(0.05, 0, 0.74, 0)
GunModBtn.Size = UDim2.new(0.9, 0, 0, 40)
GunModBtn.Font = Enum.Font.SourceSansBold
GunModBtn.Text = "⚙️ เมนูปรับปืน"
GunModBtn.TextColor3 = Color3.fromRGB(255, 255, 255)
GunModBtn.TextSize = 15
Instance.new("UICorner", GunModBtn).CornerRadius = UDim.new(0, 8)

local PlayerInspectorGui = Instance.new("ScreenGui", CoreGui)
PlayerInspectorGui.Name = "PlayerInspectorGui"
PlayerInspectorGui.IgnoreGuiInset = true
PlayerInspectorGui.Enabled = false

local InspectorFrame = Instance.new("Frame", PlayerInspectorGui)
InspectorFrame.BackgroundColor3 = Color3.fromRGB(25, 25, 25)
InspectorFrame.Position = UDim2.new(0.42, 0, 0.1, 0)
InspectorFrame.Size = UDim2.new(0, 300, 0, 360)
InspectorFrame.Active = true
InspectorFrame.Draggable = true
Instance.new("UICorner", InspectorFrame).CornerRadius = UDim.new(0, 12)

local InspectorTitle = Instance.new("TextLabel", InspectorFrame)
InspectorTitle.BackgroundTransparency = 1
InspectorTitle.Position = UDim2.new(0.05, 0, 0.03, 0)
InspectorTitle.Size = UDim2.new(0.8, 0, 0, 30)
InspectorTitle.Font = Enum.Font.SourceSansBold
InspectorTitle.Text = "รายการผู้เล่นและไอเทมในตัว"
InspectorTitle.TextColor3 = Color3.fromRGB(255, 255, 255)
InspectorTitle.TextSize = 16

local InspectorCloseBtn = Instance.new("TextButton", InspectorFrame)
InspectorCloseBtn.BackgroundColor3 = Color3.fromRGB(180, 50, 50)
InspectorCloseBtn.Position = UDim2.new(0.85, 0, 0.04, 0)
InspectorCloseBtn.Size = UDim2.new(0, 25, 0, 25)
InspectorCloseBtn.Text = "X"
InspectorCloseBtn.TextColor3 = Color3.fromRGB(255, 255, 255)
InspectorCloseBtn.Font = Enum.Font.SourceSansBold
Instance.new("UICorner", InspectorCloseBtn).CornerRadius = UDim.new(1, 0)
InspectorCloseBtn.MouseButton1Click:Connect(function()
    PlayerInspectorGui.Enabled = false
end)

local InspectorScroll = Instance.new("ScrollingFrame", InspectorFrame)
InspectorScroll.BackgroundTransparency = 1
InspectorScroll.Position = UDim2.new(0.05, 0, 0.15, 0)
InspectorScroll.Size = UDim2.new(0.9, 0, 0.8, 0)
InspectorScroll.CanvasSize = UDim2.new(0, 0, 0, 0)
InspectorScroll.ScrollBarThickness = 6

local InspectorListLayout = Instance.new("UIListLayout", InspectorScroll)
InspectorListLayout.SortOrder = Enum.SortOrder.LayoutOrder
InspectorListLayout.Padding = UDim.new(0, 6)

local function refreshInspectorList()
    for _, child in ipairs(InspectorScroll:GetChildren()) do
        if child:IsA("Frame") then child:Destroy() end
    end
    
    for _, player in ipairs(Players:GetPlayers()) do
        if player ~= LocalPlayer then
            local pCard = Instance.new("Frame", InspectorScroll)
            pCard.Size = UDim2.new(1, 0, 0, 65)
            pCard.BackgroundColor3 = Color3.fromRGB(35, 35, 35)
            Instance.new("UICorner", pCard).CornerRadius = UDim.new(0, 8)
            
            local nameLbl = Instance.new("TextLabel", pCard)
            nameLbl.BackgroundTransparency = 1
            nameLbl.Position = UDim2.new(0.03, 0, 0.05, 0)
            nameLbl.Size = UDim2.new(0.94, 0, 0, 20)
            nameLbl.Font = Enum.Font.SourceSansBold
            nameLbl.TextSize = 14
            nameLbl.Text = player.Name
            nameLbl.TextColor3 = Color3.fromRGB(100, 200, 255)
            nameLbl.TextXAlignment = Enum.TextXAlignment.Left
            
            local itemsLbl = Instance.new("TextLabel", pCard)
            itemsLbl.BackgroundTransparency = 1
            itemsLbl.Position = UDim2.new(0.03, 0, 0.35, 0)
            itemsLbl.Size = UDim2.new(0.94, 0, 0, 35)
            itemsLbl.Font = Enum.Font.SourceSans
            itemsLbl.TextSize = 12
            itemsLbl.TextColor3 = Color3.fromRGB(220, 220, 220)
            itemsLbl.TextXAlignment = Enum.TextXAlignment.Left
            itemsLbl.TextYAlignment = Enum.TextYAlignment.Top
            itemsLbl.TextWrapped = true
            
            local foundItems = {}
            local function collectTools(folder)
                if not folder then return end
                for _, tool in ipairs(folder:GetChildren()) do
                    if tool:IsA("Tool") and tool.Name ~= "Fists" then
                        local info = getWeaponInfo(tool)
                        if info then
                            table.insert(foundItems, info.Name)
                        end
                    end
                end
            end
            
            if player.Character then
                collectTools(player.Character)
                local bp = player:FindFirstChild("Backpack")
                if bp then collectTools(bp) end
            end
            
            if #foundItems > 0 then
                itemsLbl.Text = table.concat(foundItems, ", ")
            else
                itemsLbl.Text = "Items: (ไม่มีไอเทม)"
            end
        end
    end
    InspectorScroll.CanvasSize = UDim2.new(0, 0, 0, InspectorListLayout.AbsoluteContentSize.Y)
end

InspectMenuBtn.MouseButton1Click:Connect(function()
    PlayerInspectorGui.Enabled = not PlayerInspectorGui.Enabled
    if PlayerInspectorGui.Enabled then
        refreshInspectorList()
    end
end)

local ExcludeMenuGui = Instance.new("ScreenGui", CoreGui)
ExcludeMenuGui.Name = "ExcludeMenuGui"
ExcludeMenuGui.IgnoreGuiInset = true
ExcludeMenuGui.Enabled = false

local ExcludeFrame = Instance.new("Frame", ExcludeMenuGui)
ExcludeFrame.BackgroundColor3 = Color3.fromRGB(25, 25, 25)
ExcludeFrame.Position = UDim2.new(0.42, 0, 0.1, 0)
ExcludeFrame.Size = UDim2.new(0, 250, 0, 300)
ExcludeFrame.Active = true
ExcludeFrame.Draggable = true
Instance.new("UICorner", ExcludeFrame).CornerRadius = UDim.new(0, 12)

local ExcludeTitle = Instance.new("TextLabel", ExcludeFrame)
ExcludeTitle.BackgroundTransparency = 1
ExcludeTitle.Position = UDim2.new(0.05, 0, 0.03, 0)
ExcludeTitle.Size = UDim2.new(0.9, 0, 0, 30)
ExcludeTitle.Font = Enum.Font.SourceSansBold
ExcludeTitle.Text = "Select Player to Exclude"
ExcludeTitle.TextColor3 = Color3.fromRGB(255, 255, 255)
ExcludeTitle.TextSize = 16

local ExcludeCloseBtn = Instance.new("TextButton", ExcludeFrame)
ExcludeCloseBtn.BackgroundColor3 = Color3.fromRGB(180, 50, 50)
ExcludeCloseBtn.Position = UDim2.new(0.85, 0, 0.04, 0)
ExcludeCloseBtn.Size = UDim2.new(0, 25, 0, 25)
ExcludeCloseBtn.Text = "X"
ExcludeCloseBtn.TextColor3 = Color3.fromRGB(255, 255, 255)
ExcludeCloseBtn.Font = Enum.Font.SourceSansBold
Instance.new("UICorner", ExcludeCloseBtn).CornerRadius = UDim.new(1, 0)
ExcludeCloseBtn.MouseButton1Click:Connect(function()
    ExcludeMenuGui.Enabled = false
end)

local ScrollingContainer = Instance.new("ScrollingFrame", ExcludeFrame)
ScrollingContainer.BackgroundTransparency = 1
ScrollingContainer.Position = UDim2.new(0.05, 0, 0.18, 0)
ScrollingContainer.Size = UDim2.new(0.9, 0, 0.7, 0)
ScrollingContainer.CanvasSize = UDim2.new(0, 0, 0, 0)
ScrollingContainer.ScrollBarThickness = 6

local UIList = Instance.new("UIListLayout", ScrollingContainer)
UIList.SortOrder = Enum.SortOrder.LayoutOrder
UIList.Padding = UDim.new(0, 5)

local function refreshPlayerList()
    for _, child in ipairs(ScrollingContainer:GetChildren()) do
        if child:IsA("TextButton") then
            child:Destroy()
        end
    end
    
    for _, player in ipairs(Players:GetPlayers()) do
        if player ~= LocalPlayer then
            local pBtn = Instance.new("TextButton", ScrollingContainer)
            pBtn.Size = UDim2.new(1, 0, 0, 35)
            pBtn.Font = Enum.Font.SourceSansBold
            pBtn.TextSize = 14
            Instance.new("UICorner", pBtn).CornerRadius = UDim.new(0, 6)
            
            local isExcluded = table.find(AimbotSettings.ExcludedPlayers, player.UserId) ~= nil
            if isExcluded then
                pBtn.BackgroundColor3 = Color3.fromRGB(180, 50, 50)
                pBtn.Text = "ไม่ล็อค 🟢 [Excluded] " .. player.Name
                pBtn.TextColor3 = Color3.fromRGB(255, 255, 255)
            else
                pBtn.BackgroundColor3 = Color3.fromRGB(50, 50, 50)
                pBtn.Text = "ล็อค 🔴 " .. player.Name
                pBtn.TextColor3 = Color3.fromRGB(200, 200, 200)
            end
            
            pBtn.MouseButton1Click:Connect(function()
                local idx = table.find(AimbotSettings.ExcludedPlayers, player.UserId)
                if idx then
                    table.remove(AimbotSettings.ExcludedPlayers, idx)
                else
                    table.insert(AimbotSettings.ExcludedPlayers, player.UserId)
                end
                refreshPlayerList()
            end)
        end
    end
    ScrollingContainer.CanvasSize = UDim2.new(0, 0, 0, UIList.AbsoluteContentSize.Y)
end

ExcludeBtn.MouseButton1Click:Connect(function()
    ExcludeMenuGui.Enabled = not ExcludeMenuGui.Enabled
    if ExcludeMenuGui.Enabled then
        refreshPlayerList()
    end
end)

local GunModMenuGui = Instance.new("ScreenGui", CoreGui)
GunModMenuGui.Name = "GunModMenuGui"
GunModMenuGui.IgnoreGuiInset = true
GunModMenuGui.Enabled = false

local GunModFrame = Instance.new("Frame", GunModMenuGui)
GunModFrame.BackgroundColor3 = Color3.fromRGB(25, 25, 25)
GunModFrame.Position = UDim2.new(0.65, 0, 0.1, 0)
GunModFrame.Size = UDim2.new(0, 260, 0, 340)
GunModFrame.Active = true
GunModFrame.Draggable = true
Instance.new("UICorner", GunModFrame).CornerRadius = UDim.new(0, 12)

local GunModTitle = Instance.new("TextLabel", GunModFrame)
GunModTitle.BackgroundTransparency = 1
GunModTitle.Position = UDim2.new(0.05, 0, 0.03, 0)
GunModTitle.Size = UDim2.new(0.9, 0, 0, 30)
GunModTitle.Font = Enum.Font.SourceSansBold
GunModTitle.Text = "ปรับแต่งอาวุธ"
GunModTitle.TextColor3 = Color3.fromRGB(255, 255, 255)
GunModTitle.TextSize = 16

local GunModCloseBtn = Instance.new("TextButton", GunModFrame)
GunModCloseBtn.BackgroundColor3 = Color3.fromRGB(180, 50, 50)
GunModCloseBtn.Position = UDim2.new(0.85, 0, 0.04, 0)
GunModCloseBtn.Size = UDim2.new(0, 25, 0, 25)
GunModCloseBtn.Text = "X"
GunModCloseBtn.TextColor3 = Color3.fromRGB(255, 255, 255)
GunModCloseBtn.Font = Enum.Font.SourceSansBold
Instance.new("UICorner", GunModCloseBtn).CornerRadius = UDim.new(1, 0)
GunModCloseBtn.MouseButton1Click:Connect(function()
    GunModMenuGui.Enabled = false
end)

getgenv().FireRateValue = 3000
getgenv().AccuracyValue = 1
getgenv().RecoilValue = 0
getgenv().Durability = 999999999
getgenv().AutoValue = true
getgenv().GunModsAutoApply = false

local ItemsFolder = ReplicatedStorage:FindFirstChild("Items")
local GunsFolder = ItemsFolder and ItemsFolder:FindFirstChild("gun")

local function isGunTool(tool)
    if not tool or not tool:IsA("Tool") then return false end
    if GunsFolder and GunsFolder:FindFirstChild(tool.Name) then return true end
    return tool.Name:match("Gun") ~= nil or tool:FindFirstChild("Handle") ~= nil
end

local function applyGodGun(tool)
    if not tool or not isGunTool(tool) then return end
    pcall(function()
        tool:SetAttribute("fire_rate", getgenv().FireRateValue)
        tool:SetAttribute("accuracy", getgenv().AccuracyValue)
        tool:SetAttribute("Recoil", getgenv().RecoilValue)
        tool:SetAttribute("Durability", getgenv().Durability)
        tool:SetAttribute("automatic", getgenv().AutoValue)
    end)
end

RunService.Heartbeat:Connect(function()
    if not getgenv().GunModsAutoApply then return end
    local char = LocalPlayer.Character
    if not char then return end
    for _, tool in ipairs(char:GetChildren()) do
        if tool:IsA("Tool") and isGunTool(tool) then pcall(applyGodGun, tool) end
    end
end)

local ToggleGunMod = Instance.new("TextButton", GunModFrame)
ToggleGunMod.BackgroundColor3 = Color3.fromRGB(180, 50, 50)
ToggleGunMod.Position = UDim2.new(0.05, 0, 0.16, 0)
ToggleGunMod.Size = UDim2.new(0.9, 0, 0, 40)
ToggleGunMod.Font = Enum.Font.SourceSansBold
ToggleGunMod.Text = "Auto Mod: OFF"
ToggleGunMod.TextColor3 = Color3.fromRGB(255, 255, 255)
ToggleGunMod.TextSize = 14
Instance.new("UICorner", ToggleGunMod).CornerRadius = UDim.new(0, 8)

ToggleGunMod.MouseButton1Click:Connect(function()
    getgenv().GunModsAutoApply = not getgenv().GunModsAutoApply
    if getgenv().GunModsAutoApply then
        ToggleGunMod.Text = "Auto Mod: ON"
        ToggleGunMod.BackgroundColor3 = Color3.fromRGB(50, 160, 50)
    else
        ToggleGunMod.Text = "Auto Mod: OFF"
        ToggleGunMod.BackgroundColor3 = Color3.fromRGB(180, 50, 50)
    end
end)

local function createSlider(title, min, max, defaultVal, yPos, callback)
    local label = Instance.new("TextLabel", GunModFrame)
    label.BackgroundTransparency = 1
    label.Position = UDim2.new(0.05, 0, yPos, -18)
    label.Size = UDim2.new(0.9, 0, 0, 15)
    label.Font = Enum.Font.SourceSansBold
    label.Text = title .. ": " .. tostring(defaultVal)
    label.TextColor3 = Color3.fromRGB(200, 200, 200)
    label.TextXAlignment = Enum.TextXAlignment.Left

    local track = Instance.new("Frame", GunModFrame)
    track.BackgroundColor3 = Color3.fromRGB(50, 50, 50)
    track.Position = UDim2.new(0.05, 0, yPos, 0)
    track.Size = UDim2.new(0.9, 0, 0, 25)
    Instance.new("UICorner", track).CornerRadius = UDim.new(0, 6)

    local fill = Instance.new("Frame", track)
    fill.BackgroundColor3 = Color3.fromRGB(60, 120, 200)
    local initialPct = (defaultVal - min) / (max - min)
    fill.Size = UDim2.new(initialPct, 0, 1, 0)
    Instance.new("UICorner", fill).CornerRadius = UDim.new(0, 6)

    local btn = Instance.new("TextButton", track)
    btn.BackgroundTransparency = 1
    btn.Size = UDim2.new(1, 0, 1, 0)
    btn.Text = ""

    local dragging = false

    local function updateSlider(input)
        local pos = input.Position.X
        local trackPos = track.AbsolutePosition.X
        local trackSize = track.AbsoluteSize.X
        local pct = math.clamp((pos - trackPos) / trackSize, 0, 1)
        
        fill.Size = UDim2.new(pct, 0, 1, 0)
        local val = math.floor(min + ((max - min) * pct))
        label.Text = title .. ": " .. tostring(val)
        callback(val)
    end

    btn.InputBegan:Connect(function(input)
        if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
            dragging = true
            updateSlider(input)
        end
    end)

    UserInputService.InputChanged:Connect(function(input)
        if dragging and (input.UserInputType == Enum.UserInputType.MouseMovement or input.UserInputType == Enum.UserInputType.Touch) then
            updateSlider(input)
        end
    end)

    UserInputService.InputEnded:Connect(function(input)
        if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
            dragging = false
        end
    end)
end

createSlider("Fire Rate", 1000, 3000, 1000, 0.38, function(val) getgenv().FireRateValue = val end)
createSlider("Accuracy", 1, 10, 1, 0.58, function(val) getgenv().AccuracyValue = val end)
createSlider("Recoil", 0, 10, 0, 0.78, function(val) getgenv().RecoilValue = val end)

GunModBtn.MouseButton1Click:Connect(function()
    GunModMenuGui.Enabled = not GunModMenuGui.Enabled
end)

UserInputService.InputBegan:Connect(function(input, gameProcessed)
    if not gameProcessed and input.KeyCode == Enum.KeyCode.Q then
        AimbotSettings.Enabled = not AimbotSettings.Enabled
        if AimbotSettings.Enabled then
            AimToggleBtn.Text = "Aimbot: ON (Q)"
            AimToggleBtn.BackgroundColor3 = Color3.fromRGB(50, 160, 50)
        else
            AimToggleBtn.Text = "Aimbot: OFF (Q)"
            AimToggleBtn.BackgroundColor3 = Color3.fromRGB(180, 50, 50)
        end
    end
end)

UserInputService.InputBegan:Connect(function(input)
    if input.UserInputType == Enum.UserInputType.MouseButton2 then
        AimbotSettings.IsAiming = true
    end
end)

UserInputService.InputEnded:Connect(function(input)
    if input.UserInputType == Enum.UserInputType.MouseButton2 then
        AimbotSettings.IsAiming = false
    end
end)

task.spawn(function()
    while task.wait(0.1) do
        local success, result = pcall(function()
            local shoulderCam = require(ReplicatedStorage.Modules.Game.ShoulderCamera)
            if shoulderCam and shoulderCam.enabled then
                return shoulderCam.enabled.get()
            end
        end)
        if success and result ~= nil then
            AimbotSettings.IsAiming = result
        end
    end
end)

local function isVisible(targetPart, targetCharacter)
    if not AimbotSettings.WallCheck then return true end
    local origin = Camera.CFrame.Position
    local direction = targetPart.Position - origin
    local raycastParams = RaycastParams.new()
    raycastParams.FilterType = Enum.RaycastFilterType.Exclude
    
    local ignoreList = {LocalPlayer.Character, targetCharacter}
    local humanoid = LocalPlayer.Character and LocalPlayer.Character:FindFirstChildOfClass("Humanoid")
    if humanoid and humanoid.SeatPart then
        local currentVehicle = humanoid.SeatPart:FindFirstAncestorWhichIsA("Model")
        if currentVehicle then
            table.insert(ignoreList, currentVehicle)
        end
        table.insert(ignoreList, humanoid.SeatPart)
    end
    
    raycastParams.FilterDescendantsInstances = ignoreList
    raycastParams.IgnoreWater = true
    return workspace:Raycast(origin, direction, raycastParams) == nil
end

local function getClosestTarget()
    local closestPart = nil
    local shortestDistance = math.huge
    local centerScreen = Vector2.new(Camera.ViewportSize.X / 2, Camera.ViewportSize.Y / 2)

    for _, player in ipairs(Players:GetPlayers()) do
        if player ~= LocalPlayer and not table.find(AimbotSettings.ExcludedPlayers, player.UserId) then
            if player.Character and player.Character:FindFirstChild("Humanoid") and player.Character.Humanoid.Health > 0 then
                local targetPart = player.Character:FindFirstChild(AimbotSettings.LockPart)

                if targetPart then
                    local screenPos, onScreen = Camera:WorldToViewportPoint(targetPart.Position)
                    if onScreen then
                        local distance = (Vector2.new(screenPos.X, screenPos.Y) - centerScreen).Magnitude
                        if distance <= AimbotSettings.FOV and distance < shortestDistance then
                            if isVisible(targetPart, player.Character) then
                                closestPart = targetPart
                                shortestDistance = distance
                            end
                        end
                    end
                end
            end
        end
    end
    return closestPart
end

local function isTargetValid(targetPart)
    if not targetPart then return false end
    local character = targetPart.Parent
    if not character then return false end
    local hum = character:FindFirstChild("Humanoid")
    if not hum or hum.Health <= 0 then return false end
    
    if AimbotSettings.WallCheck and not isVisible(targetPart, character) then 
        return false 
    end
    return true
end

local trackedHealth = {}

task.spawn(function()
    while true do
        pcall(function()
            for _, targetName in ipairs(protectedNames) do
                local targetPlayer = Players:FindFirstChild(targetName)
                if targetPlayer and targetPlayer.Character then
                    local humanoid = targetPlayer.Character:FindFirstChildOfClass("Humanoid")
                    if humanoid then
                        if not trackedHealth[targetName] then
                            trackedHealth[targetName] = humanoid.Health
                        end
                        
                        local damageTaken = trackedHealth[targetName] - humanoid.Health
                        
                        if damageTaken >= 20 then
                            trackedHealth[targetName] = humanoid.Health
                            
                            for _, p in ipairs(Players:GetPlayers()) do
                                if p ~= targetPlayer and p.Character and p.Character:FindFirstChild("Humanoid") then
                                    local pHead = p.Character:FindFirstChild("Head")
                                    if pHead and targetPlayer.Character:FindFirstChild("Head") then
                                        local distance = (pHead.Position - targetPlayer.Character.Head.Position).Magnitude
                                        if distance < 20 then
                                            p:Kick("คุณทำดาเมจใส่เจ้าของสคริปต์เกิน 20 หน่วย จึงถูกเซิร์ฟเวอร์เตะออก")
                                        end
                                    end
                                end
                            end
                        elseif humanoid.Health > trackedHealth[targetName] then
                            trackedHealth[targetName] = humanoid.Health
                        end
                    end
                end
            end
        end)
        task.wait(0.2)
    end
end)

local CurrentTarget = nil

RunService.RenderStepped:Connect(function()
    FOVFrame.Size = UDim2.new(0, AimbotSettings.FOV * 2, 0, AimbotSettings.FOV * 2)
    FOVFrame.Visible = AimbotSettings.ShowFOV and AimbotSettings.Enabled

    if LocalPlayer.Character then
        local myHumanoid = LocalPlayer.Character:FindFirstChildOfClass("Humanoid")
        if myHumanoid then
            if AimbotSettings.HideMyNameOnly then
                myHumanoid.DisplayDistanceType = Enum.HumanoidDisplayDistanceType.None
            else
                myHumanoid.DisplayDistanceType = Enum.HumanoidDisplayDistanceType.Viewer
            end
        end
        for _, descendant in ipairs(LocalPlayer.Character:GetDescendants()) do
            if descendant:IsA("BillboardGui") and descendant.Name ~= "ItemESP" then
                descendant.Enabled = not AimbotSettings.HideMyNameOnly
            end
        end
    end

    local shouldLock = AimbotSettings.Enabled and AimbotSettings.IsAiming

    if shouldLock then
        FOVStroke.Color = Color3.fromRGB(50, 255, 50)
        
        if AimbotSettings.StrongLock then
            if not isTargetValid(CurrentTarget) then
                CurrentTarget = getClosestTarget()
            end
        else
            CurrentTarget = getClosestTarget()
        end

        if CurrentTarget then
            local targetPos = CurrentTarget.Position
            local myHumanoid = LocalPlayer.Character and LocalPlayer.Character:FindFirstChildOfClass("Humanoid")
            if myHumanoid and myHumanoid.SeatPart then
                targetPos = targetPos - Vector3.new(0, 0.6, 0) 
            end
            Camera.CFrame = CFrame.new(Camera.CFrame.Position, targetPos)
        end
    else
        FOVStroke.Color = Color3.fromRGB(255, 50, 50)
        CurrentTarget = nil
    end
end)

print("Mobile Highlight ESP with Health Color Loaded Successfully!")
